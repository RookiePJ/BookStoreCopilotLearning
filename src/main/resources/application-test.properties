## Test profile settings
# Disable data.sql-todo during tests to avoid schema conflicts
spring.sql.init.mode=never

# Keep other logging quiet during tests
logging.level.root=WARN
logging.level.pjr.bookstore=INFO

